# Secrets & runtime state — NEVER commit (config.json holds the cookie + password)
config.json
config.json.tmp
*.tmp

# Uploaded club/sponsor logos (runtime state, referenced from config.json)
uploads/

# Box identity + update outcome (runtime state, survives updates)
.boxid
update-state.json

# Chromium kiosk profile
.kiosk-profile/

# Python
__pycache__/
*.pyc

# Debug scratch
/tmp/
debug-login.py
